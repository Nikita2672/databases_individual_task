{
  "name": "@db-lab/postgresql-app",
  "version": "0.0.0",
  "private": true,
  "description": "PostgreSQL integration app for generating and persisting CRDT workload",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json",
    "smoke": "tsx src/run.ts"
  },
  "dependencies": {
    "@gvsem/epistyl": "file:../../.."
  },
  "devDependencies": {
    "@types/node": "^25.3.5",
    "tsx": "^4.21.0",
    "typescript": "^5.3.0"
  }
}
